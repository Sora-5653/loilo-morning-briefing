# 朝ブリーフィング LoiLoNote非UI取得仕様書

- 版: 1.1
- 更新日: 2026-08-20
- 状態: 実装・単体試験・実アカウントでの読み取り専用試験済み

## 1. 目的

平日7:30（Asia/Tokyo）の既存朝ブリーフィングに、LoiLoNote SchoolをGmailと並ぶ独立した情報源として含める。LoiLoNote Web版の画面操作を朝の実行経路から完全に除外し、読み取り専用のHTTP収集器で、開講中授業の課題とタイムライン共有カード、特にテキストカード本文を安定して取得する。

成功条件は、画面構成や表示待ちに依存せず、取得した構造化JSONからその日の行動に関係する学校連絡と課題をブリーフィングへ反映できることである。認証切れや構造変更時は推測や画面操作へ退避せず、取得済みの正常なキャッシュを保護して部分成功または利用不能を正確に報告する。

収集器単体では、明示オプションを指定した場合に限り、授業の「共有ノート」一覧とそのフォルダー内のノートも収集する。このオプションは朝ブリーフィングの既存プロンプトや定期実行設定を変更しない。

## 2. 範囲

### 対象

- 開講中授業一覧
- 各授業の提出箱一覧、締切、本人の提出状態
- 各授業のタイムライン共有項目
- 共有されたノート文書内のテキストカード本文
- 明示オプション指定時の、各授業の共有ノート一覧とフォルダー内ノート
- 画像、PDF・文書、Webリンク、種類不明カードの安全なメタデータ
- 差分カーソル、検証済みキャッシュ、部分成功
- Gmailとの重要度付けと重複排除

### 非対象

- 課題提出、編集、削除、既読化、返信、メッセージ送信などの書き込み
- 他の生徒の提出内容・提出状況の収集
- 画像OCR、PDF本文抽出、音声・動画の解析
- 非公開APIの安定性保証
- Codexによるブラウザー、Chrome、画面操作

## 3. 取得方式

2026-08-12時点のLoiLoNote Webクライアントの静的調査から、次の読取経路を使用する。

1. Windows資格情報マネージャーから `connect.sid` セッションCookieを取得する。
2. `GET https://loilonote.app/_/` を実行し、HTML内の `initial-state` から一時的なAPI接続先、`authToken`、本人識別子をメモリ上だけで取得する。
3. 同一のCookieと一時トークンで次のGET要求を行う。

| 対象 | 要求 |
|---|---|
| 授業一覧 | `GET /api/courses/v3` |
| 提出箱 | `GET /api/courses/{courseId}/submissions/v2` |
| 本人の提出状態 | `GET /api/courses/{courseId}/submissions/{submissionNumber}/submitted` |
| タイムライン | `GET /api/courses/{courseId}/timeline_entries?omit_submission_related=true` |
| 共有ノート文書 | `GET /api/documents/{documentId}`。検証済みの `loilonote-assets.loilo.tv/document/` への署名付き転送だけを一段取得 |
| 共有ノート一覧 | `GET /api/notes/shared/v3?course_id={courseId}` |
| 共有ノートフォルダー | `GET /api/note_groups/{noteGroupId}/notes` |
| 共有ノートスナップショット | `GET /api/notes/{noteId}/snapshot?protocol_version=0.4.0`。検証済みの `loilonote-assets.loilo.tv/note_{number}/` への署名付き転送だけを一段取得 |
| 共有ノートプロトコル | `GET /api/lns_share_work`。サーバーの最新対応版と収集器の対応上限のうち低い版を選択 |

API接続先はHTTPSかつLoiLoNote関連の許可ホストに限定し、要求パスは `/api/` 以下に限定する。HTTPメソッドは実装上GET以外を拒否する。文書本体だけは、応答された署名付きURLの方式、ホスト、ポート、パス、クエリキーを検証してから一段取得する。この二段目にはCookieとAPIトークンを送らない。その他の転送は拒否する。トークンは送信直前にクエリへ付加し、ログ、例外、キャッシュ、標準出力へ残さない。

これは公開された永続API契約ではなく、現行Webクライアントが自身の表示に使う読取通信を再現したものである。したがって、構造変更を正常な空データと誤認しない厳格な検証と、既存キャッシュを消さない失敗処理を必須とする。

## 4. 認証

朝の収集器はブラウザーを起動しない。初回またはセッション失効時だけ、利用者が `scripts/setup-loilo-auth.ps1` を明示的に実行する。補助処理は一時的なPlaywrightブラウザーを表示し、利用者自身が公式ログイン画面で認証する。パスワード、学校ID、多要素認証コードをこのリポジトリやCodexへ渡さない。

認証後、補助処理は公式サイトのセッションCookieだけを標準入力経由でPythonへ渡し、Windows資格情報マネージャーの `Codex:LoiLoNote:connect.sid` に保存する。一時ブラウザーの利用者データは永続化しない。定期タスクはこの補助処理を自動起動せず、認証がない場合は `authentication_required` として終了する。

認証情報の平文ファイル、環境変数、コマンドライン引数、キャッシュへの保存は禁止する。

## 5. タイムライン、共有ノート、カード

タイムライン応答の `entries` から共有項目だけを選び、共有ノートの `document.id` を使って文書を取得する。文書はZIPとして検証し、`body` をJSONとして読み取る。`entire_body` がある場合はそれを優先し、各フレームのカードを次の共通形式に変換する。

`collect --include-shared-notes` を指定した場合、収集器は授業ごとに共有ノート一覧をページ処理する。一覧にフォルダーがある場合は、フォルダー内のノートもページ処理する。各ノートの読み取り専用スナップショットをZIPとして検証し、タイムライン文書と同じカード形式へ変換する。既定の `collect` はこの経路を実行しない。

共有ノートの一覧は最大5ページ、フォルダーは1授業につき最大100件、共有ノートは1授業につき最大1,000件とする。上限を超える応答は正常な空データとして扱わず、当該授業の共有ノート構成要素を失敗にする。

収集器が対応する共有ノートプロトコルの上限は `0.4.0` とする。収集開始時にサーバーの最新対応版を取得し、両者のうち低い版をスナップショット要求へ指定する。応答が有効なセマンティックバージョンでない場合は推測や別経路へ退避せず、共有ノート構成要素を失敗にする。

| 種別 | 出力 | 備考 |
|---|---|---|
| テキスト | `{"type":"text","text":"..."}` | `gadgets.title.text` の文字列本文を正規化して保持。補助的な `type` フィールドの有無には依存しない |
| 画像 | `{"type":"image", ...}` | OCRしない |
| PDF・文書 | `{"type":"pdf/document", ...}` | 本文を推測しない |
| Webリンク | `{"type":"web/link", ...}` | 認証情報らしいクエリを持つURLは省略 |
| その他 | `{"type":"unknown", ...}` | 推測せず種別だけ記録 |

カード数は文書ごとに最大2,000件、テキスト本文はカードごとに最大20,000文字とする。文書サイズ、ZIP内の `body` サイズにも上限を設ける。壊れたZIP、欠落した `body`、不正JSON、未知の必須構造は構造エラーとし、その授業の当該構成要素だけを失敗にする。

## 6. 課題

課題ごとに次を保持する。

- 安定した課題IDと授業ID
- 授業名、課題名、公開時刻、表示された締切
- `submitted`、`not_submitted`、`late`、`resubmission_needed`、`unknown` の状態
- 安全に確認できる補助メッセージ

状態を確認できない場合は `unknown` とし、未提出と断定しない。締切が表示されない場合も推測しない。他の生徒の提出一覧や内容は取得しない。

## 7. キャッシュと差分

既定保存先は `%LOCALAPPDATA%\LoiLoMorningBriefing` である。

- `loilonote-cache.json`: 検証済みブリーフィング入力
- `loilonote-state.json`: 授業ごとのタイムラインカーソルと成功時刻

書込みは同じディレクトリの一時ファイルへ完全に書いて同期した後、原子的に置換する。全取得失敗、空授業一覧への予期せぬ変化、不正構造では既存キャッシュを上書きしない。一部授業または一部構成要素だけ失敗した場合は、その範囲の直前キャッシュを引き継いで `partial` とする。30時間を超えたキャッシュは `stale` として、経過時間を最終ブリーフィングへ注記する。

タイムラインカーソルは当該授業のタイムライン取得が成功した場合だけ進める。課題とGmailの成功時刻はそれぞれ独立して扱う。

共有ノートを収集した実行だけ、キャッシュに `shared_notes` 配列を保存する。各要素はノートID、授業、ノート名、作成日時、本文更新日時、メタデータ更新日時、カードを保持する。一覧の更新日時が前回キャッシュと一致するノートは、前回検証済みのカードを再利用する。オプションを指定しない実行は共有ノートAPIを呼び出さず、出力に `shared_notes` を含めない。

個別スナップショットの取得だけが失敗し、同じノートの検証済み旧キャッシュがある場合は、そのノートへ `stale: true` を付けて保持し、構成要素を `partial` とする。本文更新日時とメタデータ更新日時が両方欠落しているノートは、変更なしと判定せず、実行ごとにスナップショットを取得する。収集全体が失敗した場合は直前のキャッシュ全体を保護するため、直前の成功実行で保存した `shared_notes` も残る。

## 8. ブリーフィングへの受渡し

定期タスクは次の順でのみLoiLoNoteを扱う。

1. `scripts/run-loilo-collector.ps1` を実行する。
2. 成否にかかわらず `scripts/read-loilo-briefing-input.ps1` を実行する。
3. 2の標準出力にある検証済みJSONだけを読む。
4. 画面操作やキャッシュ直接読取りへ退避しない。

テキストカード本文は、行事、持ち物、時間割変更、休校・登校、健康・安全、保護者対応、期限付き手続きなど、当日の行動に関係する連絡の判断に使用する。課題は締切超過、未提出・再提出、24時間以内、7日以内の順に優先する。両方の情報源に同じ連絡がある場合は、確実に同一と判断できるときだけ統合する。

## 9. 障害時の扱い

| 状況 | 挙動 |
|---|---|
| 認証なし・失効 | 再ログイン画面を自動表示せずLoiLoNoteだけ利用不能 |
| 429・一時的な5xx・ネットワーク障害 | 上限付きで再試行 |
| 401・403 | 認証必要として停止し、再試行しない |
| 4xx | 恒久的な要求失敗として再試行しない |
| 構造変更 | 構造エラー、正常な空データにしない |
| 一部授業の失敗 | 成功分と旧キャッシュで `partial` |
| 全授業の失敗 | 既存キャッシュを保護し、収集失敗 |
| 古いキャッシュ | `stale` と経過時間を明示 |

どの場合もCookie、トークン、トークン付きURL、個人識別子をエラーへ含めない。

## 10. 受入条件

- [x] 朝の実行経路がCodexのブラウザー・Chrome操作を使わない
- [x] GET以外をクライアント層で拒否する
- [x] 授業、提出箱、本人の提出状態、タイムライン、文書の読取経路が実装されている
- [x] テキストカード本文と主要カード種別を構造化できる
- [x] 不正構造、認証切れ、一時障害、部分失敗、全失敗を区別する
- [x] 全失敗や空応答で正常キャッシュを上書きしない
- [x] 認証情報をWindows資格情報マネージャーへ保存し、平文ファイルに残さない
- [x] 定期タスク用プロンプトが収集器のJSONだけを読む
- [x] 単体試験がすべて成功する
- [x] 実アカウントで、少なくとも1授業のタイムラインにあるテキストカード本文を読み取れる
- [x] 実API応答から取得した文書を解析し、非空本文と主要カード種別を確認できる
- [x] 実通信がGETだけであり、文書配信先へCookieとAPIトークンを転送しないことを確認できる
- [x] CodexによるWeb版画面操作を行わず受入確認できる
- [x] オプション指定時だけ共有ノート一覧とフォルダーをページ処理できる
- [x] 共有ノートスナップショットのテキストカード本文を抽出できる
- [x] 共有ノート配信先へCookieとAPIトークンを転送しない
- [x] 既存の朝ブリーフィング用プロンプトと定期実行設定を変更しない

## 11. 2026-08-12 実通信受入結果

利用者による初回認証後、非UI収集器を実アカウントに対して読み取り専用で実行した。本文、授業名、送信者名、識別子は検証ログへ出力せず、件数・型・本文長・本文ハッシュだけを確認した。

- 授業: 16
- 正規化済み課題: 92
- タイムライン共有項目: 161
- テキストカード: 160
- 非空テキスト本文: 159
- PDF・文書カード: 91
- 画像カード: 32
- Webリンクカード: 5
- 不明カード: 9
- 文書取得失敗: 0
- キャッシュ構造検証: 成功
- キャッシュ鮮度: 正常
- 自動試験: 42件成功

全授業のタイムライン取得は成功した。全体状態が `partial` なのは、題名が空の課題項目を推測で補わず除外したためである。題名を持つ正常な課題、本人の提出状態、タイムライン本文は利用可能であり、失敗した範囲と成功した範囲を区別している。

## 12. 2026-08-20 共有ノート実通信受入結果

`--include-shared-notes` を指定し、既存の朝ブリーフ用キャッシュとは別の一時キャッシュで読み取り専用収集を実行した。本文、ノート名、授業名、識別子は検証ログへ出力せず、件数、種別、本文長、本文ハッシュだけを確認した。

- 授業: 16
- 共有ノート: 1
- 共有ノート内カード: 5
- テキストカード: 3
- 非空テキスト本文: 2
- 非空本文長: 231文字、253文字
- 画像カード: 1
- PDF・文書カード: 1
- 共有ノート構成要素の失敗: 0
- 共有ノート配信先へのCookie・APIトークン転送: なし
- 自動試験: 61件成功

既存の朝ブリーフ用キャッシュ、プロンプト、定期実行設定は変更していない。受入試験に使った一時キャッシュは、検証後に削除した。
